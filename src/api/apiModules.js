// import { SYSTEMCONFIG_UPGRADE } from '@/api/apiModules'
// 按服务端模块制定前缀
const PREFIX = 'rco';
const ADMIN = `${PREFIX}/admin`;
const CLOUDDESKTOP_OSISOFILE = `${PREFIX}/clouddesktop/osIsoFile`;
const SYSTEMCONFIG_UPGRADE = `${PREFIX}/systemConfig/upgrade`;
const MSGCT_MSG = `${PREFIX}/msgct/msg`;
export {
  PREFIX,
  ADMIN,
  CLOUDDESKTOP_OSISOFILE,
  SYSTEMCONFIG_UPGRADE,
  MSGCT_MSG
};
